# Expo Go runs on a physical device/simulator, so "localhost" refers to the
# device itself, not your development machine. Use your machine's LAN IP.
# Find it with `ipconfig getifaddr en0` (Mac) or `ipconfig` (Windows).
EXPO_PUBLIC_API_URL=http://192.168.1.50:8000/api/v1
